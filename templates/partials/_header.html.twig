
<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <a class="navbar-brand" href={{ path ('home.index') }}><img src='{{ asset('assets/images/logo-svg.svg') }}'></a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" 
	aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link" href={{ path ('dish.index') }}>La carte</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href={{ path ('booking.index') }}>Réservation</a>
        </li>   

			<div class="d-flex">
				<ul class="navbar-nav me-auto">
					{% if app.user %}
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" 
							role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.fullName }}</a>
							<div class="dropdown-menu">
								<a class="dropdown-item" href="{{ path('user.edit', {id : app.user.id}) }}">Modifier mes informations</a>
								<a class="dropdown-item" href="{{ path('user.edit.password', {id : app.user.id}) }}">Modifier mon mot de passe</a>
								<div class="dropdown-divider"></div>
								{% if 'ROLE_ADMIN' in app.user.roles %}
									<a class="dropdown-item" href="{{ path('admin') }}">Administration</a>
									<div class="dropdown-divider"></div>
								{% endif %}
								<a class="dropdown-item" href="{{ path('security.logout') }}">
								<i class="fa-solid fa-arrow-right-from-bracket"></i>Déconnexion</a>


							</div>
						</li>
					{% else %}
					<div class="registration-btn">
						<li class="nav-item">
							<a type="button" class="btn btn-signin" href="{{ path('security.registration') }}">Inscription
							</a>
						</li>
					</div>
					<div class="registration-btn">
						<li class="nav-item">
							<a type="button" class="btn btn-login" href="{{ path('security.login') }}">Connexion
							</a>
						</li>
					</div>
						<div>
						</li>
					{% endif %}
					</ul>
					</div>
				</div>
			</div>
		</div>
	</div>
</nav>
{% for message in app.flashes('success') %}
    <div class="alert alert-success">
        {{ message }}
    </div>
{% endfor %}
